#include <bits/stdc++.h>

using namespace std;

bool isParathesisBalanced(char *a){

}

int main(){
       
        string inString;
        string :: iterator it;    
        cin>>inString;
        stack <char> a;
            
        }

        if(a.empty()) cout<<"balanced";
        else cout<<"unbalanced";

        stack<char>().swap(a);

    return 0;
}